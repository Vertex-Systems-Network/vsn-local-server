# PKG-04..PKG-08 Dormant Preplanning — Current-Docs Refresh

Reviewed: 2026-09-06
Canonical source observed: `79812eafdead24de88d8b3fafd19f1bfc0e1435c`
Status: **RESEARCH-ONLY / DORMANT — NO PACKAGE ACTIVATION OR IMPLEMENTATION AUTHORITY**

This refresh supplements `.ai/plans/pkg04-pkg08-parallel-preplanning-v1.md` without changing its fixed 108-task denominator, package order, task IDs, activation prerequisites, or max-five implementation concurrency. Canonical acceptance remains PKG-03 at 21/25 with 03.22 as the only dependency-ready task.

## Portfolio boundary

- Package activation remains sequential: `PKG-03 -> PKG-04 -> PKG-05 -> PKG-06 -> PKG-07 -> PKG-08`.
- Downstream work before activation is limited to research/preparation.
- No product/runtime/installer/updater/release/security/resilience implementation is authorized by this file.
- No canonical tracker, master status, active package/task, package counters, release candidate, secrets, privileged settings, or production evidence may change from this research lane.
- Activation-time tooling/actions must be re-read from current official documentation and pinned immutably where the accepted package contract requires reproducibility.

## PKG-04 — updater/recovery current-doc delta

Current Tauri v2 updater documentation still requires signed updater artifacts; update signature verification cannot be disabled. The public verification key is distributable, while the private signing key must remain external to repository content/evidence. `createUpdaterArtifacts` remains the v2 artifact-generation switch, and production updater endpoints enforce TLS unless an explicitly dangerous insecure transport option is enabled.

Preplanning consequences:
- preserve one authoritative version/trust model; do not create a second competing updater authority;
- freeze update public-key identity, signed metadata/artifact identity, HTTPS endpoint policy, replay/anti-downgrade semantics and key-rotation/recovery behavior during 04.01–04.03 activation;
- private updater signing material must never be committed, logged, embedded in evidence, or accepted from mutable untrusted code paths;
- negative acceptance must cover invalid signature, wrong key, replayed/older metadata, corrupt/partial artifacts and transport-policy violations;
- Tauri updater adoption remains optional until activation-time reconciliation proves compatibility with the existing `vsn-update` / `vsn-updater-helper` boundary.

Official reference reviewed:
- https://v2.tauri.app/plugin/updater/

## PKG-05 — macOS release current-doc delta

Apple's current direct-distribution guidance continues to require appropriate Developer ID signing for software distributed outside the Mac App Store, Hardened Runtime for notarized apps, secure timestamps, and notarization. Apple documents `notarytool` / Notary API for automated notarization and supports stapling the returned ticket to distributable software.

Preplanning consequences:
- 05.17 must keep Developer ID credentials outside Git/repository artifacts and bind only non-secret signer/notarization identities in evidence;
- exact entitlements and hardened-runtime exceptions must be minimum-scope and activation-time reviewed;
- notarization acceptance must verify submission result/log, ticket/stapling state, final artifact identity and Gatekeeper-relevant signature/notarization properties;
- direct-distribution DMG/app/installer artifacts must remain byte/hash-bound across signing, notarization and stapling evidence so a convenient rebuild cannot silently replace the accepted candidate;
- no macOS signing/notarization secret provisioning is authorized before PKG-05 activation.

Official references reviewed:
- https://developer.apple.com/documentation/security/hardened-runtime
- https://developer.apple.com/documentation/security/notarizing-macos-software-before-distribution
- https://developer.apple.com/documentation/security/customizing-the-notarization-workflow

## PKG-05/06 — provenance and supply-chain delta

GitHub artifact attestations continue to use cryptographically signed provenance claims tied to workflow/repository/environment/commit identity and Sigstore infrastructure. GitHub explicitly notes that attestations establish provenance/integrity claims but are not themselves a guarantee that an artifact is secure; policy verification remains required. GitHub also supports attestation verification and lifecycle management.

Preplanning consequences:
- provenance/SBOM evidence must be verified, not merely generated;
- accepted subject digests must equal the exact release artifacts being certified;
- retained attestation/bundle identities and verifier/tool versions should be evidence-bound where offline reconstruction is required;
- deleted/expired/unavailable attestations must not be silently substituted with a differently hashed rebuild;
- security certification should treat provenance as one supply-chain control, not as a replacement for static/dynamic/security testing.

Official references reviewed:
- https://docs.github.com/en/actions/concepts/security/artifact-attestations
- https://docs.github.com/en/actions/how-tos/secure-your-work/use-artifact-attestations

## 2026-09-06 machine-proof research delta

The following isolated GitHub-hosted audits strengthen downstream activation requirements without changing product code, package state or canonical `main`.

### PKG-04 updater/recovery

- Lock ownership race: `audit/pkg04-lock-ownership-race`, run `33970645911` PASS on Ubuntu + Windows. Evidence proves age-only stale-lock recovery can remove a live owner lock and an older guard can later remove a replacement owner's lock. Activation therefore requires owner identity/tokens plus owner-checked release/recovery.
- Rollback backup identity + Windows durability: `audit/pkg04-rollback-backup-tamper`, run `33972819237` PASS on Ubuntu + Windows. Evidence proves tampered rollback backup bytes can be restored before backup identity re-verification. It also proves the current Windows read-only `sync_all()` durability helper shape fails while a read+write handle succeeds, so Windows-correct file/rename durability semantics are an activation requirement.

### PKG-05 Linux credential/device continuity

- `audit/pkg05-linux-keyring-lifecycle`, run `33970027940` PASS on Ubuntu. Isolated credential loss causes device-identity mismatch and IPC-secret rotation under the current Linux keyring composition; the audit explicitly does not claim same-machine reboot proof. Activation must choose a reboot-persistent, least-privilege credential/device-identity custody model and later prove same-machine continuity across reboot/service lifecycle.

### PKG-06 remote authority composition

- Runtime install trust: `audit/pkg06-remote-runtime-trust`, run `33970288403` PASS. The current remote `RuntimeManage` + `runtime.install` path accepts an unsigned catalog through plain catalog loading while the trusted loader rejects that unsigned catalog. Remote runtime installation therefore requires verified signed-catalog authority or an equivalent proven local trust boundary.
- Managed-process execution: `audit/pkg06-managed-process-remote-exec`, run `33974285316` PASS. A remote-delegated `ServiceManage` principal reaches `managed_process_start` and executes a caller-selected benign host program on the isolated runner while the current remote-terminal preference only gates `terminal.*`. Security acceptance therefore requires one coherent local approval/opt-in boundary for all host-command execution aliases.

### PKG-07 audit-chain continuity

- `audit/pkg07-audit-chain-continuity`, run `33973856880` PASS. Machine evidence proves append can extend a parseable-but-invalid predecessor and paged reads can accept individually valid records with broken `previous_hash` edges. Activation must enforce append-time chain/tail integrity and cursor-boundary/every-record continuity, not per-record validity alone.

### PKG-08 workspace mutation containment

- `audit/pkg08-write-temp-symlink`, run `33974546308` PASS on Ubuntu. Artifact `9971918019` has GitHub digest `sha256:f3222bef9cf08a2766f77a1f8c736508bc24f0283f954ef9f03e60b374c6b573`.
- The isolated proof demonstrates deterministic text/binary temp-path symlink planting can modify a sentinel outside the configured workspace root, while touching no production or real user/system state.
- Activation must require no-follow/exclusive temp creation or equivalent handle-based containment, revalidation at mutation/commit boundaries, and Windows reparse/hardlink plus Linux/macOS symlink parity tests before Stable 1.0 acceptance.

These audits are research evidence only. They do not authorize PKG-04+ implementation before prerequisite package completion and must be re-bound to the exact activation candidate before acceptance.

## Activation-time stop conditions

Stop and reconcile instead of implementing if any future activation discovers:
- a material change to Tauri updater signature/key semantics;
- a material Apple signing/notarization requirement change;
- a package task that would require widening the frozen denominator/order/task IDs;
- a trust boundary that requires production secrets in mutable PR code;
- a release/provenance flow that cannot preserve exact subject-byte lineage;
- a proposed downstream implementation while its prerequisite package is not canonically COMPLETE;
- an activation candidate that still contains a mechanically proven lock, rollback, credential-custody, remote-authority, audit-chain or workspace-containment gap without an accepted remediation/evidence plan.

## Current conclusion

No frozen downstream task/order change is justified by this refresh. The existing PKG-04..08 preplanning map remains structurally valid. Current documentation and machine-proof research strengthen fail-closed boundaries around updater signing/recovery, Linux credential continuity, macOS Developer ID/notarization, remote execution/runtime trust, audit-chain continuity, workspace mutation containment, exact artifact lineage and independent provenance verification.
