# Batch 0.24 — maximum close-first sprint

VSN 0.24 closes the finite source gaps in P5 networking, P16 remote files, P18 private preview/tunnel, P26 AI engine, and P27 marketplace. P23 cloud lifecycle is materially deeper with deterministic Azure same-region snapshot-to-disk-to-VM clone orchestration, while cross-region Azure snapshot copy remains explicit. P25 remains partial because executable extension sandbox backends on Windows/macOS are still not implemented. P29 remains source-closed; P30 owns external/native certification evidence.
