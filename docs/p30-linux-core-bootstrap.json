{
  "schema_version": 1,
  "product_version": "0.33.0",
  "rust_toolchain": "1.97.0",
  "pack_id": "linux-core",
  "host": "linux",
  "commands": [
    "rustup toolchain install 1.97.0 --profile minimal --component rustfmt --component clippy",
    "rustup override set 1.97.0",
    "cargo install cargo-audit --locked",
    "cd apps/desktop && npm install --ignore-scripts",
    "cd cloud/dashboard && npm install --ignore-scripts"
  ],
  "notes": [
    "Requires dpkg-deb and root/sudo for real deb install/uninstall acceptance."
  ]
}
